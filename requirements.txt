# Motor de vectorización ColPali + FastAPI

# ColPali y dependencias del modelo
colpali-engine>=0.3.7
transformers==4.47.0
peft==0.14.0
torch>=2.0.0

# Qdrant
qdrant-client>=1.9.0

# Conversión PDF
pdf2image>=1.17.0
PyMuPDF>=1.24.0        # fitz (alternativa a pdf2image, útil para metadata)
Pillow>=10.0.0

# API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9  # necesario para UploadFile en FastAPI

# Utilidades
stamina>=24.0.0
numpy>=1.26.0
